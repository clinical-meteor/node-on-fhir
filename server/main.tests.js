/* eslint-env mocha */
import { chai } from 'meteor/practicalmeteor:chai';

describe('my server test', function () {
  it('passes because true is true', function () {
    chai.assert.equal(true, true);
  });
});